services:
  front:
    build:
      context: .
      dockerfile: Front/Dockerfile
    environment:
      # When backend runs outside this compose (host or another compose)
      VITE_API_URL: http://host.docker.internal:5000/api
    ports:
      - "5173:5173"
